description = 'demo'

dependencies {
    api(project(':common'))
    api(project(':client'))
    api(project(':server'))

    api(rootProject.ext.dependencies["netty-all"])

    testImplementation(rootProject.ext.dependencies["junit"])
}